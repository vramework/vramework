{
  "name": "@vramework/deploy-lambda",
  "version": "0.0.14",
  "license": "MIT",
  "author": "Vlandor LTD",
  "main": "dist",
  "scripts": {
    "tsc": "rm -rf dist && tsc",
    "publish": "npm publish",
    "ncu": "ncu",
    "test": "echo 'No tests'",
    "test:ci": "echo 'No tests'"
  },
  "peerDependencies": {
    "@vramework/core": "^0.0.6",
    "aws-lambda": "^1.0.7",
    "cookie": "^0.5.0",
    "path-to-regexp": "^6.2.1",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102",
    "typescript": "^5.4.2"
  }
}
