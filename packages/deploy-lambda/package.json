{
  "name": "@vramework/deploy-lambda",
  "version": "0.1.11",
  "license": "MIT",
  "author": "Vlandor LTD",
  "main": "lambda.js",
  "scripts": {
    "tsc": "tsc",
    "build": "rm -rf dist && tsc -b",
    "release": "npm run build && npm run test && npm publish",
    "ncu": "ncu",
    "test": "echo 'No tests'"
  },
  "peerDependencies": {
    "@vramework/core": "^0.1.11",
    "aws-lambda": "^1.0.7",
    "cookie": "^0.5.0",
    "path-to-regexp": "^6.2.1",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "typescript": "^5.4.2"
  }
}
