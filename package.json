{
  "name": "@vramework/mono",
  "version": "0.1.15",
  "description": "The vramework monorepo",
  "repository": "git@github.com:vlandor/vramework.git",
  "author": "yasser.fadl@gmail.com",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "tsc": "yarn workspaces foreach -A run tsc",
    "build": "yarn workspaces foreach -A run build",
    "release": "yarn workspaces foreach -A run release",
    "ncu": "yarn workspaces foreach -A run ncu -x",
    "test": "echo 'No tests'",
    "test:ci": "echo 'No tests'"
  },
  "packageManager": "yarn@4.5.0",
  "devDependencies": {
    "@types/node": "^20.11.30"
  }
}
